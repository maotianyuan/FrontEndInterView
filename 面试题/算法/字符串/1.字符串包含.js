// 字符串包含

// ABCD

// BAD

// const hasConstans = (from, to) => {
//   return `${from}`.includes(to);
// };

const hasConstans = (from, to) => {
  const fromArr = from.split();
  const toArr = to.split();
  fromArr.map((item) => {});
};

const result = hasConstans("AVCD", "VC");
console.log(result);
